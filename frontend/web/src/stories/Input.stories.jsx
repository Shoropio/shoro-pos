import React from 'react'
import { Input } from '../components/Input'

export default {
  title: 'UI/Input',
  component: Input,
}

export const Default = () => <Input value="Texto" onChange={() => {}} placeholder="Texto" />
